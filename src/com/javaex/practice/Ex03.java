package com.javaex.practice;

public class Ex03 {
	public static void main(String[] args) {
		/*
		
		 JVM 에 대한 설명으로 틀린 것 을 찾아 수정해보세요.(3)
			(1) JVM 은 java.exe 명령어에 의해 구동된다
			    - java.exe는 Java 애플리케이션을 실행하기 위해 JVM을 호출하는 명령어입니다.
			
			(2) JVM 은 바이트코드(~.class) 를 기계어로 변환시키고 구동시키다
			    -  JVM은 바이트코드를 해석하거나 JIT(Just-In-Time) 컴파일을 통해 기계어로 변환하여 실행합니다.
			
			(3) 운영체제별로 동일한 JVM 이 사용된다
			    - 운영체제에 따라 JVM의 구현이 다르며, 각 운영체제에 맞는 JVM이 필요합니다. 예를 들어, Windows, macOS, Linux 등 각각에 맞는 JVM이 존재합니다.
			    
			(4) 바이트코드(~.class)는 운영체제에 독립적이만 JVM은 운영체제에 종속적이다
			    - 바이트코드는 플랫폼 독립적이지만, JVM은 특정 운영체제에 맞게 구현되어 있습니다.
		 
		*/
		
	}
}
